<label class="label">
  {{ case.animal.name }}
</label>

{{ case.description|raw }}

<figure id="image" class="image">
  <img id="animal" src="{{ asset('images/' ~ case.animal.image) }}" />
</figure>
