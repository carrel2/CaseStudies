{% extends 'base.html.twig' %}

{% block scripts %}
	{{ parent() }}

	<script>
		$(function() {
			updateCase();
		});
	</script>
{% endblock %}

{% block body %}
	{{ form_start(form) }}
		{% if case %}
			<div>You have a session in progress: {{ case.title }}</div>
		{% else %}
			{{ form_row(form.title) }}

			<div id="case"></div>
		{% endif %}

		{{ form_rest(form) }}
	{{ form_end(form) }}
{% endblock %}
