{#
/**
 * Available variables
 * -------------------
 *
 * {{ form }} - AppBundle/Form/CaseType
 */
#}

{% extends 'admin.html.twig' %}

{% block body %}
	{{ form_start(form, {'attr': {'id': 'case'}, 'action': path('createCase')}) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
{% endblock %}
