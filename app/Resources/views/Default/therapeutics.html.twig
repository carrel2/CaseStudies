{% extends 'base.html.twig' %}

{% block body %}
  <div class="column">
  	{{ form_start(form) }}
  	{{ form_widget(form) }}
  	{{ form_end(form) }}
  </div>
{% endblock %}

{% block footer %}
	{% if app.user.location == "Hospital" %}
		<a href="{{ path('review') }}">Review</a>
	{% endif %}

	{{ parent() }}
{% endblock %}
