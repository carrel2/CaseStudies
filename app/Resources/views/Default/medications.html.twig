{% extends 'base.html.twig' %}

{% block body %}
	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
{% endblock %}

{% block footer %}
	<a href="{{ path('review') }}">Review</a>

	{{ parent() }}
{% endblock %}
